# 重构进度

## 目标
- 在不改变行为的前提下完成 codex-new 相关大文件拆分与结构优化。
- 保持 tauri command 签名、chat-agent 路由规则、UI 结构与事件流不变。
- 为后续新增 engine/能力预留可扩展的 handler/模块边界。

## 当前总览
- 状态：进行中（已进入“按目标产品形态推进”阶段）
- 完成度：6 / 6（原 refactor 计划） + 产品形态增量持续中

## 已完成事项

### 1) backend-codexnew-split
- 已将 `desktop/src-tauri/src/codex_new.rs` 的安全逻辑与 command 实现拆分：
  - 新增 `desktop/src-tauri/src/codex_new_security.rs`
  - 新增 `desktop/src-tauri/src/codex_new_commands.rs`
- `codex_new.rs` 保留对外函数签名，改为薄 wrapper，注册路径不变。

### 2) backend-codexnew-frontendstate-extract
- 已重构 `build_frontend_state`，拆分状态聚合逻辑为小函数：
  - workspace security 映射
  - active session/task 映射
  - core timeline/terminal 映射
  - live overlay 叠加
- 返回结构和排序/截断行为保持原语义。

### 3) frontend-engine-handler-table
- 已重构 `desktop/src/features/threads/hooks/useThreadMessaging.ts`：
  - send/interrupt 都改为 engine handler table 路由（`chat_agent` / `codex_core`）
  - 保持 `turnId` 前缀规则（`chat-agent:`）与状态返回语义不变。

### 4) frontend-codexnew-tabs-extract
- 已将 `CodexNewProcessWindow.tsx` 大段 tab 渲染拆分为子组件：
  - `CodexNewTimelineTab.tsx`（含 `ChatAgentRunPanel`）
  - `CodexNewChangesTab.tsx`
  - `CodexNewReviewTab.tsx`
  - `CodexNewSummaryTab.tsx`

### 5) frontend-mainapp-codexnew-wiring
- 已新增 `desktop/src/features/app/hooks/useMainAppCodexNewWiring.ts`
- `MainApp.tsx` 中 codex-new controller + props 聚合已提取到该 hook，主文件耦合降低。

### 6) frontend-layoutsurfaces-codexnew-props
- 已在 `useMainAppLayoutSurfaces.ts` 抽取 codex-new security props 构建函数。
- 减少 `buildXSurface` 内重复透传字段与条件分支。

### 7) workbench-product-shape-pass-1
- 新增 `useWorkbenchHotkeys`：
  - 统一快捷键入口，支持 `Ctrl+↑/↓` 会话切换事件派发。
- 新增 `services/uiEvents.ts`：
  - 会话导航事件常量与派发函数集中管理，减少跨组件耦合。
- `SessionWorkbench` 增强：
  - 监听会话导航事件，基于当前工作区会话列表实现前后切换激活。
  - 继续保持 `focusCodexNewSession` 的既有状态语义。
- `WorkbenchShell` 增强：
  - 增加“Refresh”按钮并接入 `refreshCodexNewChanges`，支持显式刷新。
  - 保持原布局与状态流不变，仅增加操作入口。
- `DualTreePanel` 升级：
  - 由单区域文件变化列表升级为“双树分区视图”（Original Project / Isolated Clone）。
  - 保留过滤与搜索逻辑，清晰呈现双根目录语义。
- 可聚焦性改进：
  - 左/中/右面板加入可编程聚焦（`tabIndex`），与 `Ctrl+1/2/3` 一致。

## 验证记录
- Rust：`desktop/src-tauri` 下 `cargo check` 通过。
- Frontend：本次改动文件 `ReadLints` 诊断为 0。
- Frontend：`npm --prefix desktop run typecheck` 通过。

## 风险与说明
- 本轮仅做“等价重构”，未改动 plan 文件，未引入新协议/新业务语义。
- 仓库存在历史遗留的其他区域问题（与本轮改动无关），未在本轮处理。

## 下一步建议
- 进入你先前列出的后续任务：
  - `resume_run` 真正恢复 agent loop
  - settings 中 engine 切换 UI
  - 主会话区展示 Chat Agent step cards
  - Phase 2 `core_delegate` 对接

